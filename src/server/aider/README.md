# Aider pool

This image is designed to host aider. For each test, there is a correponding aider process. (this is already stubbed out). We need to be able to connect to these running aider processes via the terminal 
