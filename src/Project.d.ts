export declare class TesterantoProject {
    tests: [string, string, string];
    features: string;
    ports: string[];
    constructor(tests: any, features: any, ports: any);
    builder(): void;
}
