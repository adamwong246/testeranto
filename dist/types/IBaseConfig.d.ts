export declare type IBaseConfig = {
    clearScreen: boolean;
    loaders: any[];
    minify: boolean;
    outbase: string;
    outdir: string;
    ports: string[];
    __dirname: string;
    devMode: boolean;
    tests: string;
    features: string;
};
