export declare const FileTreeItem: ({ name, isFile, level, isSelected, exists, onClick }: {
    name: string;
    isFile: boolean;
    level: number;
    isSelected: boolean;
    exists?: boolean;
    onClick: () => void;
}) => JSX.Element;
